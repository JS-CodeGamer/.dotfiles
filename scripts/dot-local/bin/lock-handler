#!/bin/bash

hyprlock
# Get all current wallpapers from wpaperctl and build swaylock command
# cmd="swaylock -fs fit"
#
# # Read wpaperctl output line by line
# while IFS= read -r line; do
#   # Extract monitor name and image path
#   # Format: "monitor_name: /path/to/image.jpg"
#   if [[ $line =~ ^([^:]+):\ (.+)$ ]]; then
#     monitor="${BASH_REMATCH[1]}"
#     image="${BASH_REMATCH[2]}"
#
#     # Add image argument for this output
#     cmd="$cmd --image \"$monitor:$image\""
#   fi
# done < <(wpaperctl get-all)
#
# # Execute the swaylock command
# eval "$cmd"

